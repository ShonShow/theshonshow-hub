"use client";
import { motion, AnimatePresence } from "framer-motion";
import { useState } from "react";
import clsx from "clsx";
export default function TeamPanel({members=[]}){const[open,setOpen]=useState(null);return(<div className="grid md:grid-cols-3 gap-4"><div className="md:col-span-1 space-y-2">{members.map((m,i)=>(<button key={i} onClick={()=>setOpen(open===i?null:i)} className={clsx("w-full text-left p-3 rounded-xl bg-white/5 border border-white/10 hover:bg-white/10 transition",open===i&&"bg-white/10") }><div className="font-semibold">{m.name}</div><div className="text-sm text-neutral-300">{m.role}</div></button>))}</div><div className="md:col-span-2"><AnimatePresence mode="wait">{open!==null&&members[open]&&(<motion.div key={open} initial={{opacity:0,y:8}} animate={{opacity:1,y:0}} exit={{opacity:0,y:8}} className="rounded-2xl bg-white/5 border border-white/10 p-6"><div className="grid md:grid-cols-3 gap-4 items-start"><div className="aspect-square rounded-xl bg-white/10 overflow-hidden"></div><div className="md:col-span-2"><div className="text-xl font-bold">{members[open].name}</div><div className="text-sm text-neutral-300">{members[open].role}</div><p className="mt-3 text-neutral-200 whitespace-pre-wrap">{members[open].bio||""}</p></div></div></motion.div>)}</AnimatePresence></div></div>);}